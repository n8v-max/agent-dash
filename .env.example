# Production origin of the deployed dashboard.
# Written into .env.local by scripts/setup-vercel.sh during the Vercel import.
# Intended consumers: metadataBase for OG tags, and Playwright's BASE_URL.
# On Vercel this is also available as the system var VERCEL_PROJECT_PRODUCTION_URL
# (no scheme), so app code should prefer that and fall back to this locally.
NEXT_PUBLIC_SITE_URL=https://agent-use-dash.vercel.app

# Signing key for the session JWT (R-T14). Server-side only — never prefixed
# NEXT_PUBLIC_, or the key ships to the browser and any token can be forged.
# Generate one per environment:  openssl rand -base64 32
# Local dev: written into .env.local. CI: a fixed non-production literal in
# .github/workflows/ci.yml. Production: set in the Vercel dashboard by
# scripts/setup-vercel.sh.
AUTH_JWT_SECRET=

# Pins the server's `now` to a fixed ISO 8601 instant (ticket 62). Server-side
# only — never prefixed NEXT_PUBLIC_: it decides which rows exist, and a value a
# browser could set would be a way to ask for rows the server means to withhold.
# `src/data/clock.ts` reads it in place of the wall clock and clamps it exactly as
# it clamps the wall clock, so it can move `now` back inside the observation
# window but never past its end.
#
# Set by playwright.config.ts on the server it spawns, so e2e figures are the same
# every day. Leave it UNSET everywhere else: not in .env.local unless you are
# deliberately looking at a past instant, and never in Vercel — in production the
# wall clock is the answer.
# AGENT_DASH_NOW=2026-09-08T12:00:00Z
